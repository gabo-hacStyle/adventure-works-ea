version: 2
models:
  - name: datas_vendas
    description: Tabela intermediaria com as datas registradas nas vendas.
    columns:
      - name: combineddate
        description: Todas as datas, sem duplicado.
        tests:
          - unique